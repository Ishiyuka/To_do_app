<h1><%= t('tasks.show.title') %> </h1>

<p>
  <%= t('tasks.index.list') %>
  <%= @task.list %>
</p>
<p>
  <%= t('tasks.index.detail') %>
  <%= @task.detail %>
</p>

<%= link_to t('tasks.index.to_edit'), edit_task_path(@task.id) %> |
<%= link_to t('tasks.link.back'), tasks_path %>